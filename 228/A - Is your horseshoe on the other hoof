#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int a[4];
    cin >> a[0] >> a[1] >> a[2] >> a[3];
    sort(a,a+4);
    int x = 0;
    for(int i = 0; i < 3; i++)
    {
        if(a[i] == a[i+1])
            x++;
    }
    cout << x << '
';
    return 0;
}